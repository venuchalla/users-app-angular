import { AdobeDirectiveDirective } from './adobe-directive.directive';

describe('AdobeDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AdobeDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
